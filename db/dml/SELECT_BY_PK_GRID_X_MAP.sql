SELECT grid_x_map_uid_pk, grid_uid_fk, map_uid_fk, delete_dt
FROM GRID_X_MAP
WHERE g=? AND r=? AND i=? AND d=? AND _=? AND x=? AND _=? AND m=? AND a=? AND p=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
