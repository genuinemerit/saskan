UPDATE CHAR_SET SET
char_set_name=?,
char_set_type=?,
char_set_desc=?,
delete_dt=?
WHERE c=? AND h=? AND a=? AND r=? AND _=? AND s=? AND e=? AND t=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
