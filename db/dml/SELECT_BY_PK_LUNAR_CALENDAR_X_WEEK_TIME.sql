SELECT lunar_calendar_x_week_time_uid_pk, lunar_calendar_uid_fk, week_time_uid_fk, delete_dt
FROM LUNAR_CALENDAR_X_WEEK_TIME
WHERE l=? AND u=? AND n=? AND a=? AND r=? AND _=? AND c=? AND a=? AND l=? AND e=? AND n=? AND d=? AND a=? AND r=? AND _=? AND x=? AND _=? AND w=? AND e=? AND e=? AND k=? AND _=? AND t=? AND i=? AND m=? AND e=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
