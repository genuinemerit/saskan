SELECT map_x_map_uid_pk, map_uid_1_fk, map_uid_2_fk, touch_type, delete_dt
FROM MAP_X_MAP
WHERE m=? AND a=? AND p=? AND _=? AND x=? AND _=? AND m=? AND a=? AND p=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
