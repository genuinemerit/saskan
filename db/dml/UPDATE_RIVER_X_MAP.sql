UPDATE RIVER_X_MAP SET
river_uid_fk=?,
map_uid_fk=?,
delete_dt=?
WHERE r=? AND i=? AND v=? AND e=? AND r=? AND _=? AND x=? AND _=? AND m=? AND a=? AND p=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
