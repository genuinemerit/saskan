SELECT land_body_x_land_body_uid_pk, land_body_1_uid_fk, land_body_2_uid_fk, land_land_relation_type, delete_dt
FROM LAND_BODY_X_LAND_BODY
WHERE l=? AND a=? AND n=? AND d=? AND _=? AND b=? AND o=? AND d=? AND y=? AND _=? AND x=? AND _=? AND l=? AND a=? AND n=? AND d=? AND _=? AND b=? AND o=? AND d=? AND y=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?
ORDER BY land_body_x_land_body_uid_pk ASC;
