SELECT solar_calendar_x_moon_uid_pk, solar_calendar_uid_fk, month_uid_fk, delete_dt
FROM SOLAR_CALENDAR_X_MONTH
WHERE s=? AND o=? AND l=? AND a=? AND r=? AND _=? AND c=? AND a=? AND l=? AND e=? AND n=? AND d=? AND a=? AND r=? AND _=? AND x=? AND _=? AND m=? AND o=? AND o=? AND n=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
