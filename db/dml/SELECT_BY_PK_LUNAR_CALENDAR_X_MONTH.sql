SELECT lunar_calendar_x_moon_uid_pk, lunar_calendar_uid_fk, month_uid_fk, delete_dt
FROM LUNAR_CALENDAR_X_MONTH
WHERE l=? AND u=? AND n=? AND a=? AND r=? AND _=? AND c=? AND a=? AND l=? AND e=? AND n=? AND d=? AND a=? AND r=? AND _=? AND x=? AND _=? AND m=? AND o=? AND o=? AND n=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
