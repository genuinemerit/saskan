DELETE FROM WEEK_TIME_X_DAY_TIME
WHERE w=? AND e=? AND e=? AND k=? AND _=? AND t=? AND i=? AND m=? AND e=? AND _=? AND x=? AND _=? AND d=? AND a=? AND y=? AND _=? AND t=? AND i=? AND m=? AND e=? AND _=? AND u=? AND i=? AND d=? AND _=? AND p=? AND k=?;
